# 开发说明

## AI 工具使用

本项目使用 Codex 辅助开发。主要用于阅读既有代码和接口约定、梳理实体与 Repository 的关系、生成初版实现，以及对异常日志进行定位和审查。所有生成或修改的代码均结合项目现有结构与业务规则进行了检查和调整。

具体示例：在实现库存查询时，先使用 JPA 关联和投影查询减少 N+1 查询；随后前端加载库位接口返回 500。根据服务端堆栈定位到 Jackson 正在序列化 Hibernate 懒加载代理，最终在仅供内部查询使用的 `Location.warehouse` 关联上增加 `@JsonIgnore`，而不是关闭全局 Jackson 校验或将代理直接暴露给前端。

## 已完成内容

- 入库单创建：校验商品与库位、生成 `IN-YYYYMMDD-XXX` 单号、在事务中创建入库单/明细并累加库存。
- 库存查询：商品名称或 SKU 模糊搜索、仓库筛选、后端分页、低库存前端高亮和关键词防抖。
- 入库管理页面：商品搜索选择、仓库到库位联动、多明细增删与提交校验。
- 为入库 Service 增加了成功入库、商品不存在两个单元测试用例。

## 预埋 Bug 与修复

1. 后端商品删除：原逻辑直接删除商品，可能导致库存记录成为孤立数据。现在删除前通过 `InventoryRepository.existsByProductId` 检查关联库存；存在库存时返回业务错误并拒绝删除。
2. 前端商品分页：编辑商品成功后原逻辑无条件把 `currentPage` 设为 1。现在仅在新增商品后回到第 1 页，编辑成功后保持用户当前页码。

## 实现取舍与后续改进

- 当前入库使用 Spring 事务确保订单、明细和库存更新的原子性。若部署为多实例并发写入，单号生成可进一步改为数据库序列表或独立号段表，并配合唯一约束重试。
- 可补充 `@DataJpaTest`/接口测试，并在 CI 中运行后端测试和前端构建。
- 出库功能尚未实现；可使用条件更新（`quantity >= requestedQuantity`）或悲观锁避免并发超卖。
- 商品列表当前是前端分页；商品数据量增长后可改为后端分页查询。
